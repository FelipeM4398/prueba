language: node_js
node_js:
  - '8'
branches:
  only:
    - master
before_script:
 - npm install -g @angular/cli
script:
  - ng build --prod --base-href https://FelipeM4398.github.io/prueba
  - ng test
deploy:
  provider: pages
  skip_cleanup: true
  github_token: $GITHUB
  on:
    branch: master
