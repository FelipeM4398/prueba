language: node_js
node_js:
  - '8'
branches:
  only:
    - master
before_script:
 - npm install -g @angular/cli
script:
  - ng build --prod
  - ng test
deploy:
  provider: pages
  skip_cleanup: true
  github_token: $GITHUB
  on:
    branch: master
