language: node_js
node_js:
  - '8'
branches:
  only:
    - master
before_script:
 - npm install -g @angular/cli
script:
  - ng lint
  - ng test
  - ng build --prod
deploy:
  provider: pages
  skip_cleanup: true
  github_token: $GITHUB
  local_dir: ./dist/ChampionsLeagueApp
  on:
    branch: master
